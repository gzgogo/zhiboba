<!--index.wxml-->
<view class="container">
  <tabs dataSource="{{gameTypes}}" defaultValue="{{currentType}}" bindchange="onTypeChange"/>
  <view class="games">
    <view class="game" wx:for="{{games}}" wx:key="id">
    
    </view>
  </view>
</view>
